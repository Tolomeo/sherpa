{
  "name": "@sherpa/repo",
  "version": "0.4.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "@sherpa/configs": "^0.0.0",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "turbo": "^1.11.2",
    "typescript": "5.2.2"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@sherpa/data": "^0.4.0"
  }
}
